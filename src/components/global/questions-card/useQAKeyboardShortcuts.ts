"use client";

import { useEffect, useRef, useCallback } from "react";
import { toast } from "sonner";

export function useQAKeyboardShortcuts(
  searchFocus: () => void,
  createQuestion: () => void
) {
  // Keyboard shortcuts have been disabled
  return;
}
