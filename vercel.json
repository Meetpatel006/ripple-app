{
  "version": 2,
  "buildCommand": "npm run build --no-lint",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "ignoreCommand": "if [ \"$VERCEL_ENV\" = \"production\" ]; then exit 1; else exit 0; fi",
  "rewrites": [
    { "source": "/", "destination": "/api/static-home" }
  ]
}
